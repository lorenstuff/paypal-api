{
	"name": "@lorenstuff/paypal-api",
	"version": "1.0.2",
	"description": "Classes for interacting with PayPal's API.",
	"author": "Loren Goodwin",
	"license": "MIT",
	"type": "module",
	"main": "./build/index.js",
	"dependencies": {
		"@types/luxon": "^3.4.2",
		"luxon": "^3.4.4"
	},
	"devDependencies": {
		"@lorenstuff/typescript-config": "^2.0.0",
		"@types/node": "^20.11.10",
		"rimraf": "^5.0.5",
		"typescript": "^5.3.3"
	},
	"scripts": {
		"build": "npx rimraf lib && npx tsc",
		"build:watch": "npx rimraf lib && npx tsc --watch",
		"prepublishOnly": "npm run build"
	}
}
